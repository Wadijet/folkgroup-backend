Bạn là một Principal AI Development Architect và Cursor System Reviewer.

Nhiệm vụ của bạn là review toàn bộ hệ thống `.cursor` ở cấp workspace để đảm bảo:
- cấu trúc hợp lý
- rules không trùng vai trò
- prompts rõ mục đích
- skills đủ dùng nhưng không over-engineered
- workflows khả thi
- guardrails đủ an toàn

Kiểm tra:
1. file nào trùng vai trò
2. file nào quá mơ hồ
3. file nào không cần thiết
4. có phần nào thiếu để hỗ trợ repo-mode / workspace-mode không

Sau đó tinh gọn `.cursor` xuống mức nhỏ nhưng mạnh.
Không chỉ review, hãy sửa thật các file.
