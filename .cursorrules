# Cursor Rules – FolkForm Backend

Nội dung quy tắc đầy đủ nằm tại **`.cursor/rules/folkgroup-backend.mdc`** (một nguồn duy nhất).

Cursor áp dụng Project Rules từ `.cursor/rules/`; file `.cursorrules` (legacy) chỉ trỏ tới file đó để tránh trùng lặp.
