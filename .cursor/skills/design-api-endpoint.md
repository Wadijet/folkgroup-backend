# Skill: Design API Endpoint

**Mục đích:** Thiết kế endpoint mới đúng convention.

**Cách dùng:** Khi thêm endpoint.

**Bước:**
1. Kiểm tra CRUD có sẵn — `docs/05-development/them-api-moi.md`
2. Xác định module — `docs/module-map/backend-module-map.md`
3. DTO, Handler, Service theo naming convention
4. Response format chuẩn (code, message, data, status)
5. Cập nhật `docs/api/api-overview.md` nếu cần
